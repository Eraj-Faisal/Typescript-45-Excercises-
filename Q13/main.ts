//store my favourite transports in a list and also print the separate messege for each transport. 

let FavTransports = ["car" , "HeavyBike" , "landCruiser" , "Jeep"];

FavTransports.forEach(transports => console.log (`I would like to own a ${transports}`));